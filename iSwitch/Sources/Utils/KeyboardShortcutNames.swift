import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    /// The main trigger shortcut for app switching
    /// Users press this + a letter key to switch apps
    static let triggerModifier = Self("triggerModifier")
}
